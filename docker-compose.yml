services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      KEYCLOAK_SERVER_URL: ${KEYCLOAK_SERVER_URL}
      KEYCLOAK_INTERNAL_URL: ${KEYCLOAK_INTERNAL_URL}
      KEYCLOAK_REALM: ${KEYCLOAK_REALM}
      KEYCLOAK_CLIENT_ID: ${KEYCLOAK_CLIENT_ID}
      KEYCLOAK_CLIENT_SECRET: ${KEYCLOAK_CLIENT_SECRET}
      ETCD_ENDPOINTS: ${ETCD_ENDPOINTS:-http://etcd:2379}
    depends_on:
      - keycloak
    ports:
      - "${PORT_BACKEND:-8080}:8080"

  keycloak:
    container_name: keycloak
    image: quay.io/keycloak/keycloak:26.0.7
    ports:
      - "${PORT_KEYCLOAK:-8081}:8080"
    environment:
      KC_HOSTNAME: ${KEYCLOAK_SERVER_URL}
      KC_DB: postgres
      KC_DB_URL: jdbc:postgresql://keycloak-postgres:5432/keycloak
      KC_DB_USERNAME: keycloak
      KC_DB_PASSWORD: ${KC_DB_PASSWORD}
      KC_BOOTSTRAP_ADMIN_USERNAME: ${KEYCLOAK_ADMIN}
      KC_BOOTSTRAP_ADMIN_PASSWORD: ${KEYCLOAK_PASSWORD}
    depends_on:
      - keycloak-postgres
    command: start-dev --http-port=8080 --hostname-strict=false --http-enabled=true

  keycloak-postgres:
    image: postgres:13
    environment:
      POSTGRES_DB: keycloak
      POSTGRES_USER: keycloak
      POSTGRES_PASSWORD: ${KC_DB_PASSWORD}
    ports:
      - "${PORT_KC_DB:-5433}:5432"
    volumes:
      - keycloak_postgres_data:/var/lib/postgresql/data

volumes:
  keycloak_postgres_data:
